# AdelTechTalks Motion Carousel Renderer
Reads `src/project.json`, registers a stitched `Carousel` composition and one composition per slide (`Slide01`, `Slide02`, ...). Media is copied into `public/` by `15_motion_carousel.sh`.
