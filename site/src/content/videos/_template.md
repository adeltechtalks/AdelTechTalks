---
# =============================================================================
# STRUCTURED VIDEO — copy this file, rename it, fill it in.
# =============================================================================
# The filename becomes the URL: `camera-ai.md` → /videos/camera-ai/
# Files starting with _ are ignored, so this one never publishes.
#
# ⚠️ NOTHING IS PUBLISHED FROM THIS COLLECTION YET. No real YouTube IDs exist
# to write up, and a page built on a made-up ID renders a broken thumbnail and
# a dead link. The moment there is a real video, copy this file and it appears
# on /videos, /ar/videos, the homepage, the Latest feed and /ask-index.json
# with no other change anywhere.
# =============================================================================

title: 'What the AI in your camera is actually doing'
description: 'One sentence that makes somebody want to watch it, shown in search results and on social.'
date: 2026-01-01
lang: en            # 'en' or 'ar' — an Arabic write-up is a separate file
# translationOf: camera-ai   # the other language's slug, if it exists

topic: creator-tech # ai · automation · product · tech · building · creator-tech
tags: ['cameras', 'ai']

# The bit after v= in the YouTube URL:
# https://www.youtube.com/watch?v=dQw4w9WgXcQ  →  dQw4w9WgXcQ
youtubeId: 'REPLACE_ME'
duration: '12:04'

# Optional — which build narrative this belongs to, and its place in it.
# series: vibe-coding
# part: 3

# Timestamps in SECONDS from the start. These become real deep links into the
# video, so they have to be right — check them against the finished cut.
chapters:
  - { at: 0, title: 'What we are testing and why' }
  - { at: 95, title: 'The setup' }
  - { at: 412, title: 'Where it fell over' }
  - { at: 655, title: 'The verdict' }

# What somebody should be able to DO afterwards. This is the part that has to
# survive without the video, so write it for a reader who never presses play.
takeaways:
  - 'Run a controlled A/B on any camera feature in under ten minutes.'
  - 'Recognise the two situations where scene detection makes a photo worse.'

# Slugs from src/content/prompts/ that appear on screen. They render as a
# linked list under the write-up.
promptSlugs: []

# Guides and use cases to send people to next.
related: []

draft: true
---

The write-up goes here, in Markdown. It is not a transcript — it is what you
would tell somebody who asked "what did you find?" and did not have twelve
minutes.

Anything worth quoting from the video belongs here as text, because this is the
part search engines read and the part that still works on a phone with the
sound off.
