---
# ============================================================================
# NOT PUBLISHED. The underscore keeps this out of the build; it is the SHAPE of
# a Build story, not a story. Every value below is structural / demo content.
#
# You do not need to copy this file. `npm run build "Title"` writes the required
# fields for you — see PUBLISHING.md. This is here for the OPTIONAL ones.
#
# REQUIRED:  title · description · date · question
# Everything else below has a working default.
#
# `heroShot` is NOT required: a build with no screenshot publishes and renders a
# designed empty frame. Add the real one later and it drops into the same frame.
# ============================================================================
title: 'Can I build and launch a product using AI tools?'
description: 'A build taken from idea to launch in the open.'
date: 2026-01-01
lang: en
draft: true

kind: build
status: exploring
question: 'Can I build and launch a product using AI tools?'
context: 'Taking one idea from a blank page to something people can open.'
stage: idea

series: vibe-coding
part: 1

# ---- optional from here down ------------------------------------------------
# Leave an optional field out entirely rather than empty — an empty string is a
# value, and `liveUrl: ''` is not a URL.
heroShot: ''
heroShotAlt: ''
tools: []

# related: ['a-guide-slug', 'a-prompt-slug']
#   Anything that goes with this build, in any collection. Resolved by bare
#   slug. This is how Video → Prompt → Guide → Build is wired; see
#   PUBLISHING.md. Write both ends when you want the link to work both ways.
---

## The idea

Real prose goes here.
