---
# ============================================================================
# NOT PUBLISHED. The leading underscore in the filename keeps this file out of
# the build entirely — Astro's glob loader ignores `_`-prefixed files, so this
# never becomes a route, never enters the sitemap and never appears in a list.
#
# It is here as the shape of a Gear story, taken from the frozen template, so
# that writing a real one is copy-rename-edit. Everything in it is STRUCTURAL /
# DEMO content from the design of record and must never ship as a claim.
#
# To publish a real story: copy this file, drop the underscore, replace every
# value with something true, and add a real photograph. Without `photo` the
# story stays unpublished no matter what else is filled in.
# ============================================================================
title: 'Can I Vibe Code on the go?'
description: 'Testing whether a foldable phone can carry a real build workflow.'
date: 2026-01-01
lang: en
draft: true

category: mobile-computing
product: 'Fold'
question: 'Can I Vibe Code on the go?'
context: 'Running three panes across one foldable screen — testing whether a phone can carry a real build workflow.'

status: testing
label: currently-testing
testingSince: 2026-01-01

# Real photography only. No photo, no publication.
photo: ''
photoAlt: ''

# Set `crossover:` to a Build story slug ONLY when the device is what makes
# that build possible. Leave the field out otherwise.
---

## Why I'm testing this

Real prose goes here. Sections render only when they have real content, and
the numbering recomputes — an empty section leaves no gap.
